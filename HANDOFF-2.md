# 交接文件 II：v1.5.0 → v1.8.0 的改動

接續 `HANDOFF.md`。那份寫的是專案脈絡與領域知識（代碼表、法條對應、Big5 缺字、擬辦解析原則），**請先讀完那份再讀這份**。

這份記錄使用者實際上線試用後回報的問題，以及據此做的調整。多數改動的共同主題是：**系統已經知道的事，要轉成使用者可以直接用的形式**，而不是把判斷丟回給人。

---

## 一、核心心智模型的轉變

初版的設計是「不確定就標警告，讓人確認」。實際使用後發現這造成大量無謂的點擊——使用者原話：

> 「這種職稱幾乎都一樣了 人名跟職稱都對應的很齊了 為何還要確認?」
> 「我想要減少 自己去點合符的次數」

修正後的原則：

**確認的負擔要跟確定性成正比。** 每種比對結果都要問「這個判斷有多確定」，只有真的需要人做決定的才停下來。

具體落實成兩件事：

### 1.1 比對強度分級

`autoSeal()` 決定哪些可以自動核章：

| confidence | 意義 | 自動核章 |
| --- | --- | --- |
| `exact` | 姓名完全相符 | ✅ |
| `typo` + `unique` + 非 `weak` | 唯一可確定的筆誤 | ✅ |
| `partial` + `unique` | 只寫名但名冊上唯一 | ✅ |
| `role` | 單位＋姓＋職稱三條件吻合 | ✅ |
| `titleExact` + 未交接 | 職稱完整命中且唯一 | ✅ |
| `approval` + `baseConfidence=exact` | 批核意見來源但姓名相符 | ✅ |
| `fromMemory` | 使用者自己確認過的對應 | ✅ |
| `ambiguous` | 名冊上多人符合 | ❌ 要人挑 |
| `title` + `titleTier=history` | 現職對不上，靠沿革查到 | ❌ |
| `weak` | 只寫名又錯一字 | ❌ |

**`weak` 這個旗標很重要，別拿掉。** 公文寫「怡如」而孫佩玲已離職不在名冊時，程式會把它當成「怡慈」的錯字配給張美玲——實際只吻合一個字，沒有姓氏定錨。這種必須人工確認。

判準是**實際吻合了幾個字**：

```
靖絜 → 曾筱婷    名字 2 字全中          → 放行
江雨薇 → 江宇薇  3 字中 2 字，有姓定錨  → 放行
怡如 → 怡慈      2 字只中 1 字，無姓    → 擋下
```

### 1.2 公文層級警告不連坐

`assessDoc()` 回傳的物件多了 `blockAll` 欄位：

- `blockAll: true` —— 動搖整份配對可信度：PDF 含多組主旨、OCR 辨識、姓名數≠獎度數、來文有多種獎度且查不到本校適用哪種
- `blockAll: false` —— 只描述個別列狀況：某職稱多人符合、某人事由撞號

**沒標 `blockAll` 的警告不該阻擋其他列核章。** 那些列本身就過不了 `autoSeal` 的門檻，不需要把同一份公文裡其他已確定的人一起扣住。

這個改動讓自動核章從 25/29 提升到 27/29。

---

## 二、踩過的坑（新增）

### 2.1 異體字：最難查的一個

使用者回報「有的會成功有的不會」，同一份公文裡「佳妍」失敗、「靖絜」成功。

原因是名冊上是「陳佳**姸**」(U+59F8)、公文寫「佳**妍**」(U+599D)。**肉眼幾乎分不出來，對程式是兩個字元。**

解法是 `core.js` 開頭的 `VARIANT_PAIRS` 對照表，比對前先正規化。收了 32 組常見的（妍姸、峰峯、溫温、珏玨、秘祕、蒓蓴、喆哲…）。

台灣人事資料很常遇到，**遇到沒收錄的直接加一行**，格式是把同一個字的各種寫法擺在同一個字串裡，第一個字元是正規形式。

正規化套用在三個地方：完整姓名比對、差一字判斷、只寫名的掃描。注意只寫名那段是**在正規化後的字串上找位置**（`blockNorm.indexOf(givenNorm)`），但回報 `written` 時要取原文的字元。

附帶好處：黃建宏的「蒓」正規化成「蓴」，那個字在 Big5 裡，缺字問題少一個。

### 2.2 正則捕獲群組錯位（重複踩到）

`matchByRole` 的單位比對出過兩次問題：

第一次是 `UNIT_RE.source` 本身帶括號，外層又包一層捕獲群組，導致群組編號位移，抓到的「姓」其實是單位名。要用 `(?:...)`。

第二次是把「職稱含教務」和「單位含教務」寫成 `||` 同一層，導致：

```
王大明  教師兼教務處教務主任   ← 職稱裡有「教務」
周淑貞  教師兼幼稚園主任       ← 單位欄位是「教務處」
```

兩個都通過 → 「教務林主任」變成多人符合。**職稱比單位欄位精確，必須分層比**：先找職稱寫明單位的，都沒有才退回看單位欄位。

`matchByTitle` 的第二層也有同樣的問題，一起改了。改的時候要驗證○○國小的「教導主任」→陳志豪（職稱是教務主任、單位是教導處）仍然運作——那正是需要退回第二層的情況。

### 2.3 `git checkout` 毀掉整輪修改

改多個檔案的過程中，我用 `git checkout ui.js` 想還原一個切壞的編輯，結果把該檔案整輪的修改全退掉了。

**改多檔案時不要用 checkout 還原**，用精確的字串替換。每個階段的產物都輸出到檔案，才有辦法救回來。

### 2.4 巢狀引號

主旨常見「114年度『高雄數位學園』推廣計畫」這種巢狀寫法。原本的 `[「『]([^」』]{6,60})[」』]` 會從外引號開始卻停在內引號的收尾，切出殘缺的「114年度『高雄數位學園」。

改成成對抓取：先試 `「([^「」]{4,60})」`，再試 `『([^『』]{4,60})』`。

### 2.5 pgrep 抓到自己

驗證行程有沒有殘留時，`pgrep -f fake_worker.py` 把「我自己那行含有該字串的指令」也算進去，看起來像有殘留。用 `ps -eo pid,args | grep -E "python3? .*fake_worker\.py"` 才準確。

---

## 三、新增功能

### 3.1 事由自動區別（`differentiateReasons`）

WebHR 不接受同一人有獎懲類別、事由、核定日期完全相同的兩筆，**事由撞在一起就匯不進去**。

原本是標警告叫使用者自己改，被質疑：

> 「為何你不能直接幫忙帶上不同事由」

現在會從擬辦裡各筆所在的句段抓區別詞，放進括號：

```
擬辦：1.協助辦理數位學園教師研習：陳志豪、張美玲各嘉獎1次。
      2.協助辦理數位學園成果展示：陳志豪嘉獎1次。

陳志豪  …推廣計畫（教師研習），辛勞得力
陳志豪  …推廣計畫（成果展示），辛勞得力
```

`detectScope()` 的擷取順序：

1. **競賽類關鍵字**（班級、組別、名次、科目）—— 同一位老師帶兩班時差別在這
2. **冒號分組** —— 冒號前那句是這組的工作名稱
3. **姓名後方描述** —— 「陳志豪辦理線上課程建置嘉獎1次」
4. **句段開頭描述**

`trimOverlap()` 會砍掉事由本文已經講過的字，避免「…高雄數位學園…（數位學園教師研習）」這種重複。

真的抓不出差異時才回頭標警告，且那幾位不預設核章。

### 3.2 「本人」代稱（`findSubmitter` / `matchSelfReference`）

擬辦有人會寫「本人擬敘嘉獎1次」。承辦人在批核軌跡上標得很清楚，兩種格式：

```
意見及流程 格式：      行首直接標「承辦 主管機關…資訊執秘 高秀娟(日期)」
批核軌跡及意見 格式：  簽核人的下一行才標「承辦意見：」
```

兩種都是公文明寫的，`certain: true` 可自動核章。都找不到才退回「軌跡第 1 筆通常是承辦人」，那種要人工確認。

9 份測試公文裡 8 份能明確找到承辦人。

### 3.3 職稱沿革的交接偵測

原本用「公文年度比發文年度早」判斷要不要人工確認，但**敘獎公文幾乎每一份都是在敘去年的事**，這個判準沒有鑑別力，等於把自動核章整個關掉。

改用 `titleHandover`：批核軌跡顯示這個職務**換過人**才要確認。

```
教導主任： 王大明（114/09–114/10） → 陳志豪（114/12–115/07）   → 交接過，要挑
總務主任： 周淑貞（115/05–115/06）                              → 沒交接，直接核章
```

### 3.4 評鑑等第的精準判定

評鑑類公文擬辦常只寫「本校成績為優等」，獎度另列在來文規則裡。`findFallbackAward` 會照等第去對，這種標 `precise: true`。

**即使來文列了特優／優等／甲等三種，也不算不確定**——本校適用哪種已經查到了。

只有真的查不出來時才擋，而且提示會把來文有哪幾種獎度列出來（`levels` 欄位）方便挑。

### 3.5 本機記憶（`memory.js`）

使用者手動改人員時記下「公文寫的字 → 這個人」，下次自動套用並核章。

分兩個 bucket：

- `titles` —— 職稱型（教導主任、教務林主任）。職稱會隨人事異動改變
- `names` —— 姓名型（錯字、異體字、只寫名）

三個刻意的設計：

1. **只在系統不確定時介入** —— `applyMemory` 跳過 `confidence === 'exact'`。記憶是人工輸入的，記錯會蓋掉本來正確的判斷
2. **人不在名冊上就失效** —— 呼應「名冊上沒有的人不敘獎」原則
3. **匯入是合併不是覆蓋** —— 兩台電腦各自累積的都保留，衝突取較新的

**含姓名的記憶只存 localStorage 不上傳。** 使用者選擇的架構是分兩層：不含人名的（事由寫法、獎懲類別）放雲端跨校共用，含人名的留本機可匯出。**雲端那層還沒實作**，見待辦。

### 3.6 Mac 端 OCR（獨立 repo `ocr-service`）

掃描件改用 macOS Vision framework 辨識，取代瀏覽器內的 Tesseract。

**最大價值不是準確度，是座標。** Vision 回傳帶 x/y 的 token，`tokensToText()` 能照版面還原，這正是表格式公文（學生健檢那種）需要的——pdf.js 抽表格時會弄丟欄位對應。

架構：

```
瀏覽器 ──送 PDF──► Zeabur 中繼 ◄──輪詢領件── Mac worker (Vision)
   ▲                                              │
   └────────── 回傳帶座標的 token ◄───────────────┘
```

幾個設計決定：

- **兩把金鑰分開**：`WEB_KEY` 只能建工作，`WORKER_KEY` 只能領工作。網頁的金鑰會出現在瀏覽器裡，不該有領取權限
- **只回原始 token，不在 Mac 端解析**：姓名身分證號不經過伺服器；而且解析規則常改，放瀏覽器只要更新網頁不用重新部署 worker
- **`Procfile` 用 `--workers 1`**：工作存記憶體，多 worker 之間不共享
- **服務網址與金鑰由使用者在網頁上填**，存 localStorage，不寫死在公開 repo 裡
- 連不上時自動退回 Tesseract，不影響主流程

`ocr_extract.swift` 直接沿用使用者既有的 AF_merge 專案那支，一個字沒改。

另有 `panel/panel.py` 管理面板，在 Mac 本機用瀏覽器開關 worker、看 log。只監聽 127.0.0.1（它能啟動任意 Python 程式，**絕不能改成 0.0.0.0**）。

---

## 四、介面上學到的事

這些回饋的共同點是「系統知道的事沒有轉成可用的形式」：

**候選人要能直接點。** 原本把「符合的有 王大明、陳志豪」印成純文字，等於算出答案又要使用者去 16 人的下拉裡找。改成按鈕，附職稱和在任期間。

**未核章的列不要調暗。** 原本 `opacity: .42`，但那些正是需要操作的列，看起來像停用。改成 `.78`，且下拉、按鈕、輸入框維持 100%。

**重複資訊只講一次。** 「依據○○函辦理」每個人都一樣，原本每列都印，一頁出現三次文號。改成公文層級講一次，事由輸入框只顯示本文——但 `row.reason` 仍存完整兩行，匯出邏輯不動。

**檔名當識別標。** 使用者的檔名開頭是案件編號（`04-07`、`22-29`），捲動時那個才是認出公文的依據，放在主旨**之上**、等寬字型對齊。

**版本號很有用。** 加了之後立刻派上用場——使用者截圖顯示 v1.5.0，而修正在 v1.6.0，一眼就知道是還沒部署而不是沒修好。在這之前我至少三次得從畫面上的舊文案去推測部署狀態。

---

## 五、目前狀態

版本 v1.8.0。9 份真實公文回歸測試：**27/29 自動核章**。

剩下兩筆：
- 11 水域安全 —— 擬辦沒寫獎度，從來文撈的，整份標 warn
- 12-14 喜閱網 —— 教導主任交接過，真的要人挑

檔案：

```
webhr-award-csv/
  index.html    含兩個新設定面板（Mac OCR、本機記憶）
  core.js       APP_VERSION 在開頭，改動記得同步更新
  ui.js
  memory.js     新增
  style.css
  README.md

ocr-service/    獨立 repo
  app.py, requirements.txt, Procfile, runtime.txt
  mac_worker/   worker.py, ocr_extract.swift, config.example.json
  panel/        panel.py, workers.example.json, README.md
```

`index.html` 用 query string 做快取破除（`core.js?v=9`），改動 JS 後要同步遞增，否則使用者的瀏覽器會用舊快取。

---

## 六、待辦

### 已規劃未實作

**雲端記憶（第二層）。** 事由寫法、獎懲類別這類不含姓名的記憶放 Zeabur，跨校共用。使用者已確認要這個架構。可以在 `ocr-service` 同一個服務加端點，不用另外架。要注意學校區隔（AF_merge 有 `school_key` 的概念可沿用）。

**事由 LLM 改寫。** 使用者的 Mac 有 Ollama（Hermes 框架）。規則式事由的極限很明顯：

```
協助辦理檢送本局115年度所屬機關學校推展志願服務績效評鑑成績一覽表(如附件)，請逕依權責辦理獎勵，辛勞得力
                                                                        ↑ 94 字，接近上限
```

「檢送本局」「請逕依權責辦理」是公文對學校講話的語氣，不是描述這個人做了什麼。合理的寫法是「協助辦理115年度志願服務績效評鑑相關業務，辛勞得力」。

建議只送**主旨**（公開內容不含姓名），資訊量已經足夠——那些贅語全在主旨裡。做成「潤稿」而非取代：規則式先產出，使用者按按鈕才叫模型，兩版並排比較。本機小模型會胡說八道，要留退路。

實作上是 `ocr-service` 加 `kind` 欄位分流（`ocr` / `reason`），worker 加分支呼叫 `localhost:11434`。

**手動指定用 Mac 重新辨識。** 目前只有抽不出文字的 PDF 才走 Mac OCR。有文字層但表格散掉的公文（15-18 學生健檢）仍走 pdf.js。要讓它受惠得加一個「這份改用 Mac 重新辨識」的按鈕。

### 技術債

**`tokensToRows()` 目前沒被使用。** 它是為了表格欄位切分預留的，等真的要做表格解析時才會用到。

**表格解析仍未真正實作。** `tokensToText()` 只做到「照版面還原成文字」，還沒有「辨識這是表格、切出欄位、對應姓名與獎度」。這需要用 x 座標分群找出欄位邊界。Vision 的 token 已經有這個資訊，缺的是演算法。

**獎懲類別的關鍵字判斷未經實務驗證。** 預設 A02，比賽→A07、研習→A06、研究→A05。使用者說「你可以判斷」，但沒有實際核對過。

**代理教師的適用法規未確認。** 教師成績考核辦法適用對象是「編制內專任合格教師」，代理教師嚴格說不適用。目前照套 CA 並在介面標示提醒。

---

## 七、與使用者合作的補充

延續 `HANDOFF.md` 第八節，這輪的觀察：

**他會用截圖回報，但截圖不一定是最新版。** 版本號加上去之後好很多。收到截圖先看版本，別急著除錯。

**他丟真實公文 PDF 比描述問題有效得多。** 「04-06 午餐可溯源」「65-73 性平」那兩份直接解掉兩個猜不出來的問題。缺資訊時主動要原始檔。

**他的判斷通常是對的，即使講得簡略。** 「這種職稱幾乎都一樣了 為何還要確認」「怡如工友這種也沒什麼爭議」——這些都是準確的批評，照做就對了。

**但也要守住該守的。** 他說「錯字比對得出來就給過」時，我照做後測出「離職者的名字會被誤配」的風險，加了 `weak` 旗標擋住。他沒有反對——**合理的保留他能接受，前提是講清楚為什麼**。

**他問「這樣就夠了嗎」時是真的在問，不是要你附和。** 例如問要不要上 Zeabur，我的判斷是不需要（純前端才是這個工具的優點），他就沒有再堅持。

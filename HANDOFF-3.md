# 交接文件 III：v1.8.0 → v1.26.0

接續 `HANDOFF.md`（領域知識）與 `HANDOFF-2.md`（早期改動）。**請依序讀完那兩份再讀這份。**

這個階段的特徵是：使用者實際上線，每產出一批 CSV 就丟回來檢查。多數改動不是新功能，而是修掉真實資料才暴露出來的問題。**這個回饋迴圈比任何靜態分析都有效，請維持它。**

---

## 一、最重要的一件事：語料

`core.js` 裡多數關於「事由該怎麼寫」的規則，都是從真實語料歸納的，不是猜的：

| 來源 | 筆數 | 層級 |
| --- | --- | --- |
| 上級機關歷年敘獎令（.doc 轉檔） | 951 | 市府 |
| CPAB1209R 歷年獎懲明細表 | 1761 | 學校 |

**兩批的用語不一樣，學校端的才是主要依據。**

### 統計結果（動任何事由規則前請先看這個）

**結尾用語**（學校端 1761 筆）：

```
辛勞得力    1043  59%
備極辛勞     169   9%
圓滿達成任務  117   6%
圓滿成功      61   3%
```

上級機關端「辛勞得力」佔 75%，比例更集中。

**開頭動詞**（學校端）：

```
辦理 368   擔任 338   協辦 293   參與 89
協助 84    協助辦理 54   指導 46   主辦 40   承辦 37
```

**「協助辦理」只有 54 筆，不是主流。** 初版一律用它是錯的，`pickVerb()` 就是為此而寫：帶學生參賽→指導、選務導護→擔任、主要承辦→承辦、協辦人員→協辦，其餘才是協助辦理。

**句子結構**：兩段式 47%、三段式 42%。目前產出全是兩段式，符合主流。

**其他發現**：

- 45% 的事由用「」標計畫名稱 —— 所以引號處理很重要
- 178 筆帶數據門檻（施測率達100%、登錄率達90%）—— `draftReason` 會抓進事由
- 括號的正確用法是**工作階段**：`(畫作徵選階段)` `(畫作展覽階段)` `(頒獎典禮階段)`

### 語料怎麼重建

原始檔是 `03_本局敘獎_10604移交_.7z`（使用者提供，含 331 個 .doc）。轉檔方式：

```bash
pip install py7zr --break-system-packages
python3 -c "
import py7zr
with py7zr.SevenZipFile('xxx.7z','r') as z:
    z.extract(path='x', targets=[n for n in z.getnames() if n.lower().endswith('.doc')])
"
# LibreOffice 逐檔轉，一次批次會逾時，要分批每批 30~35 個
libreoffice --headless --convert-to 'txt:Text (encoded):UTF8' --outdir t "$f"
```

**注意 UTF8 過濾器**。用 `--convert-to txt` 會得到一堆 `?`。331 個檔最後成功 301 個，其餘是舊格式。

---

## 二、事由清理：八道規則的由來

`cleanSubject()` 把公文主旨清成可用的事由核心。**每一道都是踩到問題才加的，別合併簡化。**

執行順序有意義，改動前先讀懂為什麼是這個順序：

1. **符號雜訊** —— PDF 抽文字會把符號讀成注音符號（`網路假期ˇ上網飆`）
2. **發語詞** —— 有關、為、關於、茲
3. **轉發語氣** —— 檢送、(如附件)
4. **受獎對象子句** —— `…之現職教師及行政人員敘獎1案`
5. **產出類子句** —— `…成績及達標學校名單`
6. **案件性質後綴** —— 敘獎案、獎勵案
7. **行文雙方指稱** —— 貴校、本局所屬
8. **殘留動詞與名詞化後綴**

### 順序陷阱（踩過兩次）

**第 4 條必須在第 6 條之前。** 否則「敘獎1案」先被砍掉，「…之現職教師及行政人員」的「人員」也會被吃掉，留下半截的「…之現職教師及行政」。

**第 5 條必須在第 8 條之前。** 否則「結果」「名單」先被砍，留下孤零零的「統計」「優勝」。

### 兩個必須保護的字

**「案」不能無差別砍。** 「實施方案」「精進方案」是計畫名稱的一部分，用 `(?<![方專議提個本該此草結專])案$` 排除。

**開頭數字不能無差別砍。** 清理檔名編號時用 `^\d+(?:[-–~]\d+)*\s*[-–_.、]\s*`，只砍「編號＋分隔符」的組合。用 `^[\d\-_.\s]+` 會把「113學年度」的年度吃掉。

### 引號路徑會跳過清理（重要）

`draftReason` 有兩條路：

```js
if (quoted) core = quoted[1];        // 引號一命中就直接用
else        core = cleanSubject(...); // 清理只在這條路
```

主旨若寫成 `檢送本市「115年度『高雄數位學園』網路假期ˇ上網飆寒假作業」活動之…`，引號內含 `ˇ`，走上面那條路就沒清到。

**解法是把符號清理提到 `parseDocMeta` 解析主旨時就做**，兩條路都涵蓋。如果之後要加新的清理規則，先想清楚該放哪一層。

---

## 三、Big5 造字：一個容易被改回去的修正

`Big5.build()` 有這幾行：

```js
const cp = ch.codePointAt(0);
if (cp >= 0xE000 && cp <= 0xF8FF) continue;   // 跳過私用區
```

**不要拿掉。**

台灣人事系統大量使用造字（本機自訂字元），瀏覽器的 `TextDecoder` 會把那些 Big5 碼位對應到 Unicode 私用區（U+E000–F8FF）。如果收進反查表，編碼時會原樣輸出，CSV 裡就是亂碼——實際踩過，一批 107 筆有 16 處。

拿掉這幾行的話對照表會從 13,493 字變成 19,710 字，看起來「支援更多字」，實際上是把亂碼寫進檔案。

現在的行為是當成缺字，匯出時跳提示。使用者可以：
- 直接匯出（姓名欄變 `?`，但 WebHR 會用身分證號從現職檔帶出正確姓名）
- 改用 UTF-8

---

## 四、重複檢查：三層防線

WebHR 以「身分證號＋獎懲類別＋事由」判定重複，**一筆撞號會讓整批匯入失敗**。所以有三層：

### 第一層：單份公文內（`differentiateReasons`）

同一人多筆時抓區別詞放進括號。區別詞的擷取順序：

1. 工作階段（`○○階段`）
2. 班級（三年甲班）
3. 組別（低年級組）
4. 梯次場次
5. 名次（優等、第一名）
6. 科目

**搜尋範圍必須限制在句段內**（`segmentAt`），否則第二筆會抓到第一筆的區別詞。**同句段多人時要取距離最近的**，否則兩人拿到同一個。這兩個都踩過。

### 區別詞的品質檢查（`usableScope`）

抓到的東西不一定能用。以下一律拒絕：

- 含有名冊上其他人的姓名 —— 曾產出「（行政人員以及協助教師敘獎人員為王大明主任、李小華）」這種
- 行文說明的特徵：敘獎、名單、人員為、以及、如下
- 只是個職稱

抓不到堪用的就改用（其1）（其2），並標 `scopeAuto`，那幾筆不預設核章。

### 第二層：跨公文（`dedupeAcrossDocs`）

單份公文各自編的（其1）在不同公文之間會撞號。所有公文解析完之後要再做一次全域檢查。

**鍵值要先剝掉既有序號再分組。** 直接拿全文當鍵值的話，`其1 其2 其2 其3` 這種會修不掉——踩過。

### 第三層：匯出前（`exportCsv`）

真的還有重複就擋下來不讓匯出，並列出是哪幾位。

---

## 五、公文層級警告不連坐

`assessDoc()` 回傳的物件有 `blockAll`：

- `true` —— 動搖整份配對可信度：PDF 含多組主旨、OCR 辨識、姓名數≠獎度數、來文有多種獎度且查不到本校適用哪種
- `false` —— 只描述個別列：某職稱多人符合、某人事由撞號

**沒標 `blockAll` 的警告不該阻擋其他列核章。** 那些列本身就過不了 `autoSeal` 的門檻。這個改動讓自動核章從 25/29 提升到 27/29。

### 評鑑類的特例

評鑑公文擬辦常只寫「本校成績為優等」，獎度另列在來文規則。`findFallbackAward` 會照等第去對，這種標 `precise: true`。

**即使來文列了特優／優等／甲等三種也不算不確定** —— 本校適用哪種已經查到了。

---

## 六、其他改動速覽

**學校代碼自動帶入**（`schools.js`）。345 所高雄市學校，資料來源是使用者提供的 `compareab.xls`。名冊表頭有學校名稱就自動查代碼。簡稱（○○國小）也查得到，但只在唯一相符時才採用。

**法規欄位**。公務人員的條點項款目全部留空（原本填 999/99 哨兵值，容易讓人誤以為真的引用第 999 條），但**適用法規名稱必填，留空 WebHR 會拒絕匯入**。教師的「目」做成下拉選單，列出教師成績考核辦法第6條第2項完整各目（115.1.12 修正版，嘉獎 10 目、記功 9 目），概括款目排最前並標「（常用）」。

**審議表 Excel**（`exportReview`）。格式比照 WebHR 產的版本，第一欄是流水號。取代進系統產表那一步。

**未敘獎清單**（`exportSkipped`）。分兩區塊：解析出但未核章（附公文主旨、檔號、未核章原因）、名冊上完全沒出現的人。一律 UTF-8，因為是給人用 Excel 開的。

**異體字正規化**（`VARIANT_PAIRS`）。「林佳蓉」和「林佳蓉」肉眼難分，對程式是兩個字元。收了 32 組，遇到沒收錄的直接加一行。

**檔名當主旨備援**。OCR 讀壞主旨時（中文比例 <60% 或出現連續 6 個以上拉丁字母）改用檔名。

**到訪計數**（Abacus）。這是工具唯一會對外發出的請求，只送一個固定 key。`VISIT_KEY` 設成空字串就完全不連外。

**本機記憶**（`memory.js`）。含姓名的修正只存 localStorage，可匯出匯入。**雲端那層（不含姓名的事由寫法、獎懲類別）使用者確認要做但尚未實作。**

**範例檔**。`範例_人員基本資料表.xlsx` 加三份範例公文，全部虛構。網頁上有「使用範例名冊」「使用範例公文」兩個按鈕，`fetch` 抓下來包成 `File` 餵進正常流程。**點公文那顆時若還沒載名冊會自動補上**，否則會跳「請先上傳名冊」。

---

## 七、樣式：兩份合併過

v1.26.0 時使用者自己改寫了整份視覺（深綠系），內嵌在 `index.html` 的 `<style>` 裡。

合併時發現**新版少了 171 個選擇器**，全是 STEP 3 逐筆核對區的樣式——那區要上傳檔案後才顯示，所以改的人不會發現。

現在的狀態：
- `index.html` 215 行，只有結構
- `style.css` 1200 行，前 605 行是使用者的設計，後面是補回的元件樣式

**變數對應**（舊 → 新）：`--indigo → --green`、`--vermil → --danger`、`--stone → --ink-soft`、`--hair → --line`。

之後如果又有人改樣式，**務必檢查 STEP 3 那區**，或用這段指令比對選擇器：

```bash
python3 -c "
import re
def sels(t):
    return {p.strip() for m in re.finditer(r'([^{}]+)\{', re.sub(r'/\*[\s\S]*?\*/','',t))
            for p in m.group(1).split(',') if p.strip() and not p.strip().startswith('@')}
old=sels(open('style.css.bak').read()); new=sels(open('style.css').read())
print('遺漏:', sorted(old-new))
"
```

---

## 八、測試

`fixtures/` 不在 repo（含真實姓名），需要時重建：

```bash
python3 -c "
import subprocess, json, glob, os
out={}
for f in sorted(glob.glob('/path/to/pdfs/*.pdf')):
    out[os.path.basename(f)] = subprocess.run(['pdftotext',f,'-'],
        capture_output=True).stdout.decode('utf-8','replace')
json.dump(out, open('fixtures/pdftext.json','w'), ensure_ascii=False)
"
```

**目前基準：9 份真實公文，27/29 自動核章。** 改動後跑一次，數字掉了就是有退步。

剩下兩筆：11 水域安全（擬辦沒寫獎度）、12-14 喜閱網（教導主任交接過，真的要人挑）。

範例檔也是測試資料：3 份公文 13 筆，**13/13 全部自動核章**。

---

## 九、待辦

### 尚未實作

**雲端記憶（第二層）。** 事由寫法、獎懲類別這類不含姓名的記憶放 Zeabur 跨校共用。使用者已確認要這個架構，可以在 OCR 服務同一個服務加端點。

**表格式公文解析。** 唯一沒解的難題。`tokensToText()` 只做到照版面還原成文字，還沒有「辨識這是表格、切出欄位、對應姓名與獎度」。需要用 x 座標分群找欄位邊界，Vision 的 token 已經有這個資訊。

**Mac OCR 部署。** 程式寫好在 `ocr-service/`，使用者的 Zeabur 目前部署的是敘獎網頁本身（部署錯了）。正確做法是在同一個 repo 加 `ocr-service/` 資料夾，Zeabur 設 Root Directory 指向它。`ui.js` 的 `MAC_OCR_SERVER` 已填 `https://webhr-award-csv.zeabur.app`。

**事由 LLM 改寫。** 規則式已到七八成，邊際效益遞減。要做的話只送主旨（不含姓名），做成「潤稿」而非取代，兩版並排讓使用者選。

### 未經實務驗證

**獎懲類別的關鍵字判斷。** 預設 A02，比賽→A07、研習→A06、研究→A05。但語料顯示 **A02 佔 99.3%**（441 筆裡 438 筆），關鍵字切換可能是多餘的。使用者說「你可以判斷」，沒有實際核對過。

**代理教師的適用法規。** 教師成績考核辦法適用對象是「編制內專任合格教師」，代理教師嚴格說不適用。目前照套 CA 並在介面標示提醒。

---

## 十、與使用者合作（補充）

延續前兩份的觀察：

**每次改完他會實際跑一批，把 CSV 丟回來。** 這個迴圈抓出了造字、重複、序號撞號、符號雜訊、引號路徑漏清理——全部是靜態分析看不出來的。**主動請他這樣做。**

**看到截圖先確認版本號。** 頁尾有 `v1.x.x`。他曾經連續三批 CSV 都是舊版產的，我一直以為是程式有 bug。版本號就是為此加的。

**他會自己改東西。** v1.26.0 的視覺是他改的，改得比原版好。但他改的時候不會注意到隱藏區塊的樣式，接手時要主動檢查。

**他的直覺通常對。** 「為何還要確認」「這種也沒什麼爭議」「主旨是寫到1案但事由把1列進去」——這些都是準確的問題回報。

**但該守的要守住。** 例如他說「錯字比對得出來就給過」，照做後測出「離職者的名字會被誤配」的風險，加了 `weak` 旗標擋住。他沒有反對——**合理的保留他能接受，前提是講清楚為什麼。**

**打錯字很常見，看不懂就問。** 例如「這個是讓位」其實是「這樣的結果」。不要自己猜著往下做。
